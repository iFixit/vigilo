apiVersion: v1
kind: ConfigMap
metadata:
  name: vigilo-urls
data:
  urls.json: |
    {{- toJson .Values.urls | nindent 6 }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: vigilo-lh-config
data:
  lh-config.js: |
    {{ .Values.lhConfig | nindent 6 }}
