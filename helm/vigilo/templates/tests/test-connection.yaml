apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vigilo.fullname" . }}-test-connection"
  labels:
    {{- include "vigilo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vigilo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
